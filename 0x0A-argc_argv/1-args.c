#include <stdlib.h>
#include <stdio.h>

/**
 * main - its the main function of my codoe
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: int
**/

int main(int argc, char *argv[])
{
(void)argv;

printf("%d\n", argc);
return (0);
}

#include "main.h"
#include <string.h>

/**
 * _strlen - find the length of a string
 * @s:pointer to the string
 * Return : unsigned integer
*/


int _strlen(char *s)
{
int i;
i = strlen(s);
return (i);
}
